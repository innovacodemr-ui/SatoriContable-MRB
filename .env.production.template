# ===================================================================
# ARCHIVO DE CONFIGURACIÓN DE PRODUCCIÓN - SATORI
# ===================================================================
# INSTRUCCIONES:
# 1. Copiar este archivo al servidor Hetzner como: /root/satori/.env
# 2. Cambiar TODOS los valores marcados con <CAMBIAR>
# 3. Ejecutar: chmod 600 /root/satori/.env (solo root puede leer)
# 4. Desplegar con: docker compose --env-file .env up -d
# ===================================================================

# --- DJANGO CORE ---
DJANGO_SECRET_KEY=<CAMBIAR: Generar con: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())">
DEBUG=False

# --- CIFRADO (CRÍTICO PARA CAMPOS SENSIBLES) ---
# Generar con: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=<CAMBIAR: Usar generate_clean_env.py o el comando de arriba>

# --- GOOGLE OAUTH (ROTAR INMEDIATAMENTE) ---
# IMPORTANTE: NUNCA subir estos valores a Git
# 1. Ir a: https://console.cloud.google.com/apis/credentials
# 2. Crear nuevas credenciales OAuth 2.0
# 3. Revocar las credenciales antiguas INMEDIATAMENTE
GOOGLE_CLIENT_ID=<CAMBIAR: Nuevo Client ID de Google Cloud Console>
GOOGLE_SECRET=<CAMBIAR: Nuevo Client Secret de Google Cloud Console>

# --- BASE DE DATOS (OPCIONAL - Ya está en docker-compose) ---
# DB_NAME=satori_db
# DB_USER=satori_user
# DB_PASSWORD=<CAMBIAR: Contraseña segura>
# DB_HOST=db
# DB_PORT=5432

# --- DIAN / FIRMA DIGITAL (Si aplica) ---
# DIAN_SOFTWARE_ID=
# DIAN_SOFTWARE_PIN=
# DIAN_CERTIFICATE_PATH=
# DIAN_TEST_MODE=False

# ===================================================================
# DEPLOYMENT: 
# scp .env.production root@178.156.215.106:/root/satori/.env
# ssh root@178.156.215.106 "chmod 600 /root/satori/.env"
# ===================================================================
